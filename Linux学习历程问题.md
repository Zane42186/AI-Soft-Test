# Linux 学习历程问题追踪

> 📦 本文档记录 Linux 模块学习过程中的练习评审、错误分析与改进建议。
> 对应教材：[Liunx.md](Liunx.md)

---

## 📋 从前序模块（功能测试 Days 1~4）继承的持续关注项

> 以下是在功能测试学习阶段暴露的弱项，在 Linux 模块学习中需持续关注和改进：

| 编号 | 弱项 | 表现 | 在 Linux 中如何体现 |
|------|------|------|---------------------|
| 🔴 **HW-01** | **边界方向混淆** | 下边界的内点/离点方向反复搞反（Day 2 金额 50.00、Day 3 金额 200） | Linux 命令中的数值参数（如 `head -n`、`tail -n`、`chmod` 权限位）需精确理解起始位置 |
| 🔴 **HW-02** | **严重程度判断** | 涉及资金/核心功能的 Bug 误判为 S4（轻微） | Linux 中涉及 `rm -rf`、`chmod 777` 等危险操作也需判断风险等级 |
| 🟡 **HW-03** | **术语精准度** | 八大要素名称不准（"测试标题"→"用例标题"、"代表数据"→"测试数据"） | Linux 命令英文全称记忆（如 `ls` = List、`chmod` = Change Mode） |
| 🟡 **HW-04** | **用例完整性** | 等价类只考虑范围、不考虑数据类型格式 | Linux 命令学习中注意覆盖各种参数组合和边界情况 |

> 📌 以上弱项的详细评审记录见 [学习历程问题.md](学习历程问题.md)

---

# 第六天：Linux 文件系统与基本操作

## 评审概要

| 练习 | 状态 | 主要问题 |
|------|------|----------|
| 练习一：命令匹配 | ⚠️ 有 3 处需修正 | `ll` 非标准命令、`less` 缺少搜索步骤、管道符多余反斜杠 |
| 练习二：权限解读 | ⚠️ 有 1 处需修正 | scripts 的其他人权限漏了"读"（r-x 不是 x） |
| 练习三：模拟排查日志 | ⚠️ 有 2 处可改进 | `ls -ltr` 缺少 `-h`、第 5 题文件名不一致 |
| 自检清单 | ⚠️ 仅填写了 2/13 项 | 大部分自检项留空未作答 |

---

## 逐题评审

### 练习一：命令匹配

| 题号 | 场景 | 你的答案 | 评审 |
|------|------|----------|------|
| 1 | 想知道当前在哪个目录 | `pwd` | ✅ **正确。** `pwd` = Print Working Directory，最直接的定位命令。 |
| 2 | 查看 `.log` 文件并显示大小 | `ll /var/log/*.log` | ⚠️ **有 2 个小问题。** ① `ll` 是 `ls -l` 的别名（alias），在大部分发行版中默认配置了但**不是标准命令**——面试和正式脚本中应写 `ls -l`；② 题目要求"显示文件大小"，配合 `-h`（human-readable）更佳：`ls -lh /var/log/*.log` 可以把 2048 字节显示为 `2.0K`。 |
| 3 | 进入 `/opt/app/config` | `cd /opt/app/config` | ✅ **正确。** 使用了绝对路径，标准写法。 |
| 4 | 创建 data 和 logs 目录 | `mkdir -p /tmp/test/{data,logs}` | ⭐ **超出预期！** 同时使用了 `-p`（递归）和 `{ }` 花括号展开（批量创建），一行搞定，非常优雅。 |
| 5 | 复制配置文件并备份 `.bak` | `cp file.txt file.txt.bak` | ✅ **语法正确。** 用 `cp` 生成 `.bak` 备份是标准操作。💡 小提示：实战中文件名应具体（如 `cp config.yml config.yml.bak`），不然别人不知道你备份了哪个文件。 |
| 6 | 实时监控日志看新错误 | `tail -f app.log \| grep "error"` | ⚠️ **有 2 个小问题。** ① 管道符前的反斜杠 `\|` 多余——在 bash 中直接用 `|` 即可，`\|` 会在某些 Shell 中被当作转义；② 搜索关键词 `"error"` 是小写，常见的 ERROR 日志是大写，建议用 `grep -i "error"`（忽略大小写）或直接用 `grep "ERROR"`。 |
| 7 | 翻看 500MB 日志文件，搜索 "NullPointerException" | `less app.log` | ⚠️ **不完整。** `less app.log` 只是"打开了文件"，**缺少搜索步骤**。题目要求搜索特定异常名，完整操作是：`less app.log` 然后按 `/NullPointerException` 回车开始搜索，按 `n` 跳下一个。建议答案写为：`less app.log` → `/NullPointerException`。 |
| 8 | 查看配置文件前 20 行 | `head -n 20 config.yml` | ✅ **正确。** `head -n` 精确指定行数，标准用法。 |

---

### 练习二：权限解读

#### 权限位速查

```
drwxr-xr-x  → scripts  |  -rwxr--r--  → deploy.sh
-rw-------  → secret.key |  drwxr-x---  → logs
```

| 文件 | 类型 | 所有者权限 | 组权限 | 其他人权限 | 含义 | 评审 |
|------|------|-----------|--------|-----------|------|------|
| scripts | 目录 | 读-写-执行 | 读-执行 | 执行 | 目录，所有者可以进入和查看并创建和删除目录下文件，组成员可以查看目录下文件和进入目录，其他人只可进入 | ⚠️ **其他人权限漏了"读"。** `r-x` 包含 **r（读）** 和 x（执行），不只是执行。正确：其他人可以**查看目录内容**（ls）并可进入目录（cd）。 |
| deploy.sh | 文件 | 读写执行 | 读 | 读 | 文件，所有者可查看修改执行文件，其他人和组成员只可读 | ✅ **正确。** `rwx` = 读+写+执行，`r--` = 只读。 |
| secret.key | 文件 | 读写 | 无 | 无 | 文件，所有者可查看和修改文件，其他人无权限 | ✅ **正确。** `rw-------` = 只有所有者可读写，组和其他人完全无权限。这是私钥/密钥文件的最佳实践权限。 |
| logs | 目录 | 读写执行 | 读执行 | 无 | 目录，所有者可查看和进入目录，并可以在目录下创建删除目录和文件。组成员可查看和进入目录，其他人无权限 | ✅ **完美。** `rwx` + `r-x` + `---` 解读精准，含义描述覆盖了所有三类用户的权限。 |

#### 修正对照：scripts 的其他人权限

```
   r  -  x
   │     │
   │     └── 执行（x）：可以 cd 进入该目录
   └── 读（r）：可以 ls 查看目录内容

你的答案："只可进入" → 只有 x
正确答案："可查看目录内容并可进入" → r + x
```

---

### 练习三：模拟排查日志

| 题号 | 你的答案 | 评审 |
|------|----------|------|
| 1. 进入日志目录 | `cd /var/log/myapp/` | ✅ **正确。** 绝对路径进入，标准操作。 |
| 2. 查看文件列表（按时间排序 + 大小） | `ls -ltr` | ⚠️ **功能正确但缺细节。** `-ltr` 确实按时间排序（`-t`）且反向显示（`-r`，最新的在最下面），也显示了文件大小（`-l` 的第 5 列）。但缺少 `-h`（human-readable）：`ls -ltrh` 会把 `2097152` 显示为 `2.0M`，可读性显著提升。 |
| 3. 今天的日志文件 + 查看最后 50 行 | `app-2026-07-08.log tail -n 50 app-2026-07-08.log` | ✅ **正确。** 文件名 `app-2026-07-08.log` 格式完全正确（今天 = 2026-07-08），`tail -n 50` 用法标准。💡 格式提示：文件名和命令写在了同一行但没有分隔符，建议分行写或加一个空格分号。 |
| 4. 实时监控 ERROR | `tail -f app.log \| grep "ERROR"` | ✅ **正确。** 管道 + `grep` 过滤是测试工程师最常用的日志监控组合。⚠️ 小问题同练习一：`\|` 的反斜杠多余，直接 `|` 即可。 |
| 5. 搜索并统计 Timeout | `grep -c "Timeout" app.log` | ⭐ **超出预期！** 你直接用了下一天才会讲的 `grep -c`（count 模式），而且语法完全正确。这说明你提前查了资料，主动学习能力很棒。💡 小提示：文件名写的是 `app.log`，但第 3 题中今天的日志文件名是 `app-2026-07-08.log`——实战中要保持文件名一致，避免搜错文件。 |

---

## 📋 自检清单评审

| 自检项 | 你的回答 | 评审 |
|--------|----------|------|
| 测试工程师学 Linux 的 3 个场景 | ① 大部分软件在 Linux 服务器下 ② 查看日志定位 bug ③ 运行自动化脚本 ④ 部署环境 | ⭐ **列出 4 个，超出要求！** 覆盖了教材中的核心场景。 |
| 终端提示符各部分含义 | ① user=用户名 ② host=主机名 ③ ~=当前目录 ④ $=普通用户 | ✅ **4/4 完全正确。** 解读精准，`~` 和 `$` 的含义都到位。 |
| 如何查看命令帮助 | （未填写） | ⚠️ **建议补充。** 答案应为：`命令 --help`（简要帮助）或 `man 命令`（详细手册）。这是自学 Linux 最重要的元技能。 |
| 基本目录导航命令 | （未填写） | ⚠️ **建议补充。** 答案应为：`pwd`（查看位置）、`ls`（列出内容）、`cd`（切换目录）。 |
| 绝对路径 vs 相对路径 | （未填写） | ⚠️ **建议补充。** 绝对路径从 `/` 开始（如 `/var/log`），相对路径从当前目录开始（如 `./logs` 或 `../logs`），类比 GPS 坐标 vs "往前走右转"。 |
| mkdir -p 和 { } 用法 | （未填写） | ⚠️ **建议补充。** 练习一中你已正确使用了 `mkdir -p /tmp/test/{data,logs}`，说明掌握了！只需在自检这里确认一下。 |
| rm -rf 风险认知 | （未填写） | ⚠️ **建议补充。** 答案应为：`rm -rf /` 会删除整个系统，`rm -rf *` 删除当前目录全部内容。安全习惯：先 `ls` 确认再 `rm`，或用 `rm -i` 开启确认。 |
| cp 备份、mv 重命名 | （未填写） | ⚠️ **建议补充。** 练习一第 5 题已正确使用 `cp` 备份，说明掌握了。 |
| cat/less/head/tail 场景区分 | （未填写） | ⚠️ **建议补充。** `cat`=小文件全文，`less`=大文件分页浏览，`head`=看开头 N 行，`tail`=看末尾 N 行 + 实时跟踪。 |
| tail -f 实时跟踪 | （未填写） | ⚠️ **建议补充。** 练习一第 6 题和练习三第 4 题已正确使用，说明掌握了。 |
| less 搜索和翻页 | （未填写） | ⚠️ **建议补充。** 翻页：Space/PgDn 向下、b/PgUp 向上；搜索：`/关键词` 向下搜、`?关键词` 向上搜、`n` 下一个、`N` 上一个。 |
| 权限 -rw-r--r-- 解读 | （未填写） | ⚠️ **建议补充。** 练习二已练习过，应能说出：`-`=普通文件，`rw-`=所有者可读写，`r--`=组只读，`r--`=其他人只读。 |

> 📌 自检清单 13 项中你只填写了 2 项。虽然**练习题的完成度很高**（说明核心技能你确实掌握了），但自检清单的目的是**让你用自己的话复述一遍**，这能帮你发现"我以为会了但其实说不清楚"的知识点。建议花 10 分钟把空着的 11 项补上，不需要长篇大论，一两句话就行。

---

## 总结

| 评价维度 | 结果 |
|----------|------|
| **练习一** | 8 题中 4 题完全正确、3 题有小瑕疵（`ll` 非标准、`less` 缺搜索、管道转义）、1 题语法正确但可优化 |
| **练习二** | 4 个文件中 3 个权限解读完全正确、1 个漏了其他人"读"权限 |
| **练习三** | 5 题中 3 题完全正确（含 1 题超出预期）、2 题有细节可改进 |
| **自检清单** | 2/13 项已填写，已填部分质量高，11 项建议补充 |

### 需要修正的重点

1. 🔴 **（必须理解）权限 r-x ≠ x**：练习二中 scripts 的其他人权限是 `r-x`（可读可进入），不是"只可进入"（x）。这个错误和之前功能测试模块的"看漏条件"（HW-04）一脉相承——读到后半截 `x` 就忘了前面的 `r`。🔧 **记忆法**：权限三位一体，读权限（r）永远在第一位，只要看到 `r-x` 就要想到"有 r"。

2. 🟡 **（建议）放弃 `ll`，习惯 `ls -l`**：`ll` 是别名不是标准命令，面试写 `ll` 可能被追问，脚本中 `ll` 可能不存在。从现在起统一用 `ls -l` / `ls -lh`。

3. 🟡 **（建议）补全自检清单**：11 项空白中有多项已在练习中证明你掌握了（如 `mkdir -p`、`tail -f`、权限解读），只需花 10 分钟用自己的话写一两句即可——这会让你对 Linux 第一天的知识体系有一个完整回顾。

4. 🟡 **（建议）less 搜索是"两步操作"**：`less` 打开文件只是第一步，搜索 `/关键词` 才是第二步。以后题目问"搜索某个内容的命令"，一定要把搜索步骤写全。

### 跨模块追踪

| 历史弱项 | 本次是否复现 | 说明 |
|----------|-------------|------|
| 🔴 HW-01 边界方向混淆 | 未复现 ✅ | 本次不涉及边界值判断 |
| 🔴 HW-02 严重程度判断 | 未复现 ✅ | 本次不涉及缺陷分级 |
| 🟡 HW-03 术语精准度 | ⚠️ **部分复现** | `ll` 代替 `ls -l`、scripts 权限漏"读"——都是不精准的表现 |
| 🟡 HW-04 用例完整性 | 未复现 ✅ | 本次练习完成度整体良好 |

> 🎯 **整体评价**：第六天 Linux 开局很不错！练习一和练习三的核心命令（`pwd`、`cd`、`mkdir -p`、`tail -f`、`head -n`、`grep -c`）用得都很熟，尤其是 `mkdir -p /tmp/test/{data,logs}` 和 `grep -c` 已经是**加分项级别**的表现。权限解读也基本到位（4 中 3 正确），说明你对 `rwx` 体系的理解框架是对的。
>
> 今天最大的收获是：你已经能**把命令和测试场景对应起来**——知道查日志用 `tail -f`、找错误用 `grep`、翻大文件用 `less`——这才是测试工程师学 Linux 的核心价值，不是背命令而是**知道什么场景用什么工具**。
>
> 把自检清单补上、权限漏读那个点纠正一下，就可以进入第七天了！第七天的文本三剑客（`grep`/`sed`/`awk`）是你今天已经提前接触过的 `grep` 的深度展开，会很过瘾。🚀

---

# 第七天：文本处理三剑客 — grep / sed / awk

## 评审概要

| 练习 | 状态 | 主要问题 |
|------|------|----------|
| 练习一：grep 实战 | ⚠️ 7 中 5 正确 | `-b` 和 `-B` 混淆（大小写敏感）、第 7 题管道逻辑完全错误 |
| 练习二：sed 实战 | ❌ 5 中 1 正确 | `s`/`d` 混用、`-i` 误用、`^#`≠空行、`-n` 缺失 |
| 练习三：awk 实战 | ❌ 5 中 1 正确 | `=`≠`==`、`end`≠`END`、字符串缺引号、`NR` 未用、多处语法错误 |
| 练习四：组合技 | ❌ 3 中 1 正确 | 管道 AND/OR 逻辑混淆、awk 列号错误、统计方法缺失 |
| 自检清单 | ⚠️ 部分填写 | 4/11 项有内容，grep 部分质量高，sed/awk 部分未展开 |

---

## 逐题评审

### 练习一：grep 实战

| 题号 | 需求 | 你的答案 | 评审 |
|------|------|----------|------|
| 1 | 找出所有 ERROR | `grep "ERROR" app.log` | ✅ **正确。** 基础搜索，语法标准。 |
| 2 | ERROR + 行号 | `grep -n "ERROR" app.log` | ✅ **正确。** `-n` 显示行号，精准。 |
| 3 | 统计 ERROR 次数 | `grep -c "ERROR" app.log` | ✅ **正确。** `-c` 统计匹配行数。Day 6 已提前掌握！ |
| 4 | PaymentService 不区分大小写 | `grep -i "PaymentService" app.log` | ✅ **正确。** `-i` 忽略大小写。 |
| 5 | ERROR + 前 1 行 | `grep -b 1 "ERROR" app.log` | ❌ **关键错误：`-b` ≠ `-B`！** `-b` 是 `--byte-offset`（显示字节偏移量），`-B 1` 才是显示匹配行**前** 1 行。🔧 **记忆法**：`-A`fter / `-B`efore / `-C`ontext 都是**大写字母**，小写 `-b` 是完全不同的选项。正确：`grep -B 1 "ERROR" app.log` |
| 6 | 排除 DEBUG | `grep -v "DEBUG" app.log` | ✅ **正确。** `-v` 反向匹配，排除 DEBUG。 |
| 7 | 统计每种异常出现次数 | `grep -c "NullPointerException" \| grep -c "TimeoutException" app.log` | ❌ **语法和逻辑双重错误。** ① 管道前 `grep -c "NullPointerException"` 没有指定文件，管道后的 `app.log` 只属于第二个 grep；② 管道逻辑：管道传递的是文本内容，不是命令拼接。正确做法参考教材中的模式：`grep "ERROR" app.log \| awk '{print $NF}' \| sort \| uniq -c \| sort -rn`。或者更简单：`grep -oE "NullPointerException\|TimeoutException\|Connection refused" app.log \| sort \| uniq -c` |

> 📌 **Day 7 核心教训 #1**：grep 选项**严格区分大小写**。`-b`（byte offset）、`-B`（before context）、`-A`（after）、`-C`（context）——大小写不同，含义完全不同。

---

### 练习二：sed 实战

| 题号 | 需求 | 你的答案 | 评审 |
|------|------|----------|------|
| 1 | 替换 ERROR 为 \*\*ERROR\*\* | `sed 's/ERROR/**ERROR**/g' app.log` | ✅ **完全正确。** `s/旧/新/g` 标准写法，`g` 全局替换。 |
| 2 | IP 替换为 [内网IP] | `sed -i 's/192.168.1.100/[内网IP]/g' app.log` | ⚠️ **`-i` 误用。** 题目说"不改文件，只输出"，但 `-i` 会**直接修改原文件**。正确：去掉 `-i`：`sed 's/192.168.1.100/[内网IP]/g' app.log`。💡 **判断标准**：题目明确说"不改文件" → 永远不加 `-i`。 |
| 3 | 删除所有空行 | `sed '/^#/d' app.log` | ❌ **正则表达式错误。** `^#` 匹配以 `#` 开头的行（注释行），不是空行。**空行的正则**是 `^$`（行首紧接着行尾，中间无任何字符）。正确：`sed '/^$/d' app.log`。🔧 **记忆**：`^$` = 行首(`^`)紧挨行尾(`$`) = 空行。 |
| 4 | 只输出第 5~10 行 | `sed '5,10p' app.log` | ❌ **缺少 `-n`，会导致重复输出。** 不加 `-n` 时 sed 默认打印每一行，再额外打印 5~10 行一次——结果每行出现一次，5~10 行出现两次。正确：`sed -n '5,10p' app.log`。💡 **sed 黄金规则**：只想输出指定行 → 必须加 `-n`。 |
| 5 | 替换 localhost + 备份 | `sed -i.bak 's/localhost/192.168.1.50/d' config.yml` | ❌ **`s` 和 `d` 不能混用！** `s/旧/新/` 是替换命令，`d` 是删除命令，两者互斥。末尾的 `/d` 会被 sed 当作替换目标的一部分。正确：`sed -i.bak 's/localhost/192.168.1.50/g' config.yml`。🔧 **记忆**：`s` 命令格式永远是 `s/pattern/replacement/flags`，flag 用 `g`（全局），不是 `d`。 |

> 📌 **Day 7 核心教训 #2**：sed 命令有严格的语法规则——`s` 用于替换（加 `g` 全局），`d` 用于删除，不能混搭。`-n` + `p` 是打印指定行的固定搭配。

---

### 练习三：awk 实战

| 题号 | 需求 | 你的答案 | 评审 |
|------|------|----------|------|
| 1 | 输出日期和服务名 | `awk '{print $1,$4}' app.log` | ✅ **完全正确。** 列提取语法标准，逗号自动加空格分隔。 |
| 2 | ERROR 的日期和消息 | `awk '$3 == ERROR {print $1,$2,$4,$5}' app.log` | ❌ **字符串缺引号。** `ERROR` 没有加双引号，awk 会把它当作**变量名**（值为空），条件永远不成立。正确：`awk '$3 == "ERROR" {print $1,$2,$4,$5}' app.log`。🔧 **awk 铁律**：比较字符串必须加引号 `"字符串"`，数字可以不加。 |
| 3 | 统计总行数 | `awk '{count++} end {print "总行数:" , count}' app.log` | ⚠️ **`end` 应为 `END`（大写）。** awk 关键字严格区分大小写，`END` 是关键字，`end` 会被当作普通变量。另外语法需要两个块分开：`awk '{count++} END {print "总行数:", count}' app.log` |
| 4 | 每行前加行号 | `awk '{count++},{[print [count] $0}' app.log` | ❌ **多处语法错误。** ① 花括号不配对，逗号多余；② `[count]` 方括号在 awk 中是数组语法，不是字符串拼接；③ 用 `NR`（内置行号变量）比自己维护计数器更简单。正确：`awk '{print "["NR"]", $0}' app.log`。🔧 **awk 内置变量**：`NR` = 当前行号，不需要自己数。 |
| 5 | PaymentService 的最后一列 | `awk '$3 ="PaymentService" {print $NF}' app.log` | ❌ **两个错误。** ① `=` 是**赋值**，不是比较！`=` 会把 `$3` 的值改成 `"PaymentService"`，条件永远为真。正确用 `==`：`$4 == "PaymentService"`；② 列号不对——第 3 列是日志级别（ERROR/INFO...），服务名是**第 4 列**（`$4`）。正确：`awk '$4 == "PaymentService" {print $NF}' app.log` |

> 📌 **Day 7 核心教训 #3**：awk 中 **`=` 是赋值，`==` 是比较**（和 Python/C 一样）。**`"字符串"` 必须加引号**。**`END`/`NR`/`NF` 大写**。

---

### 练习四：组合技实战

| 题号 | 需求 | 你的答案 | 评审 |
|------|------|----------|------|
| 1 | ERROR 按服务统计次数 | `grep "ERROR" app.log \| awk '{print $4}' \| awk '$3 == "ERROR" {count++} end {print"每个服务的 ERROR 次数：",count}' app.log` | ❌ **管道逻辑断裂 + 冗余。** ① 第一个 `awk '{print $4}'` 已经只输出服务名（单列），第二个 awk 中 `$3` 不存在了；② 第二个 awk 末尾又指定了 `app.log` 作为输入，这会覆盖管道输入；③ 正确做法参考教材：`grep "ERROR" app.log \| awk '{print $4}' \| sort \| uniq -c \| sort -rn` — 管道每一步只做一件事，不要在一个 awk 里包办所有。 |
| 2 | 实时监控 ERROR 或 FATAL | `tail -f app.log \| grep "ERROR" \| grep "FATAL"` | ❌ **AND 逻辑混淆。** 管道串连多个 grep = **AND（与）**——一行必须同时包含 ERROR **且** FATAL 才能通过，这几乎不可能。需求是 **OR（或）**。正确：`tail -f app.log \| grep -E "ERROR\|FATAL"`。🔧 **记忆法**：`grep A \| grep B` = A 且 B（交集）；`grep -E "A\|B"` = A 或 B（并集）。 |
| 3 | Exception 的日期+异常名 | `grep "Exception" app.log \| awk'{print $1,$2,$NF}'` | ✅ **基本正确！** 逻辑对：grep 过滤 → awk 提取列。⚠️ 小瑕疵：`awk'{print` 少了空格，应为 `awk '{print`。这在实际终端会报 `command not found`。 |

> 📌 **Day 7 核心教训 #4**：管道中的 grep 串联是逐层过滤（AND），grep -E 的正则 `|` 才是 OR。**`sort \| uniq -c` 是分组统计的标准模式**——记住这个组合，它会反复使用。

---

## 📋 自检清单评审

| 自检项 | 你的回答 | 评审 |
|--------|----------|------|
| 三剑客各自定位 | grep=查找内容、sed=修改内容、awk=输出特定行和计算 | ✅ **正确。** 九个字精准概括。 |
| grep -i/-v/-n/-c/-E | 5 个选项全部正确解释 | ⭐ **完美！** grep 部分掌握到位。 |
| grep -A/-B/-C | 三个选项全部正确解释 | ⭐ **完美！** 但在练习一第 5 题用了 `-b` 而不是 `-B`——知道意思但写的时候大小写出错了 |
| sed s/旧/新/g | （未填写细节） | ⚠️ 练习二第 1 题正确使用了，但自检未写。补充：`sed 's/旧/新/g' 文件名`，`g` = 全局替换每行所有匹配。 |
| sed -i.bak | （未填写细节） | ⚠️ 练习二第 5 题用到了，但同时写了 `/d` 导致错误。补充：`sed -i.bak 's/旧/新/g' 文件` — 修改前备份为 `.bak`。 |
| awk 按列处理 ($1/$2/$NF) | （未填写细节） | ⚠️ 建议补充：`$1`=第1列、`$NF`=最后一列、`$0`=整行，默认按空格/Tab 分列。 |
| awk {print $1,$3} | （未填写细节） | ⚠️ 练习三第 1 题正确使用了，自检可写一句话确认。 |
| awk 统计计算 | （未填写细节） | ⚠️ 建议补充：`{sum+=$N} END {print sum}` — 在 END 块输出计算结果。注意 `END` 大写。 |
| 管道组合 grep/sed/awk | （未填写细节） | ⚠️ 练习四暴露了管道逻辑问题（AND vs OR），建议重点巩固。 |
| 独立写出搜索→提取→统计 | （未填写细节） | ⚠️ 这是 Day 7 最核心的能力。练习四第 1 题的正确答案 `grep ... | awk '{print $N}' | sort | uniq -c | sort -rn` 就是标准模板，建议背下来。 |

> 📌 自检清单同样只有 4/11 项有实质内容。好消息是 grep 相关部分（前 3 项）回答质量很高。sed 和 awk 部分虽然在练习中暴露了语法错误，但概念理解（替换/列处理）是对的——主要是**语法细节**需要多练。

---

## 总结

| 评价维度 | 结果 |
|----------|------|
| **练习一** | 7 题中 5 题正确（grep 基础扎实），2 题错误（`-b`/`-B` 大小写混淆 + 管道统计方法缺失） |
| **练习二** | 5 题中仅 1 题完全正确，4 题有错误（`s`/`d` 混用、`-i` 误用、`^#`≠`^$`、`-n` 缺失） |
| **练习三** | 5 题中仅 1 题完全正确，4 题有错误（`=`≠`==`、`end`≠`END`、字符串缺引号、`NR` 未用） |
| **练习四** | 3 题中 1 题基本正确，2 题有逻辑错误（管道 AND/OR 混淆、awk 列号+统计方法） |
| **自检清单** | 4/11 项有内容，grep 部分解释精准 |

### 需要修正的重点

#### 🔴 错误根因分析：三大"大小写/符号敏感"类错误

这次 20 题中出现了多个同根因错误，核心原因只有一个：**Linux 严格区分大小写和符号**。

| 错误类型 | 你的写法 | 正确写法 | 出现题号 |
|----------|----------|----------|----------|
| grep 选项大小写 | `-b`（字节偏移） | `-B`（前文上下文） | 练习一 Q5 |
| awk 关键字大小写 | `end` | `END` | 练习三 Q3 |
| awk 赋值 vs 比较 | `=`（赋值） | `==`（比较） | 练习三 Q5 |
| awk 字符串引号 | `$3 == ERROR` | `$3 == "ERROR"` | 练习三 Q2 |
| sed 多命令混用 | `s/.../.../d` | `s/.../.../g` 或 `/.../d` | 练习二 Q5 |
| sed 正则概念 | `^#`（注释行） | `^$`（空行） | 练习二 Q3 |

> 🔴 **这是进入 Linux 模块以来的第一个"新错误类型"**——和之前功能测试的"边界方向混淆"（HW-01）不同，这是一个**语法层面的系统性疏漏**。好消息是这很容易纠正——终端会立刻报错，敲一次就记住了。

#### 具体修正清单

1. 🔴 **（必须）背下大小写对照表**：
   - grep: `-A`fter / `-B`efore / `-C`ontext — 全大写
   - awk: `END` / `BEGIN` / `NR` / `NF` — 全大写
   - awk: `==` 是比较，`=` 是赋值
   - 字符串在 awk 中必须加引号 `"..."`

2. 🔴 **（必须）sed 命令的铁律**：
   - `s` 替换用 `/g` 结尾，不是 `/d`
   - `d` 删除单独用：`/模式/d`
   - 只打印指定行 → 必须加 `-n` + `p`
   - 不改文件 → 永远不加 `-i`

3. 🔴 **（必须）管道逻辑**：
   - `grep A | grep B` = A **且** B（越来越少）
   - `grep -E "A|B"` = A **或** B
   - `sort | uniq -c | sort -rn` = 分组统计降序排列（记住这个标准模式！）

4. 🟡 **（建议）把 `grep ... | awk '{print $N}' | sort | uniq -c | sort -rn` 这条"咒语"背下来**——它是测试工程师从日志中提取统计信息的最常用组合，面试和工作都是高频用法。

### 跨模块追踪

| 历史弱项 | 本次是否复现 | 说明 |
|----------|-------------|------|
| 🔴 HW-01 边界方向混淆 | 未复现 ✅ | 本次不涉及边界判断 |
| 🔴 HW-02 严重程度判断 | 未复现 ✅ | 本次不涉及缺陷分级 |
| 🟡 HW-03 术语精准度 | ⚠️ **明显复现** | `-b`≠`-B`、`end`≠`END`、`=`≠`==`、`^#`≠`^$` — 符号层面的精准度问题集中爆发 |
| 🟡 HW-04 用例完整性 | ⚠️ **部分复现** | 部分命令不完整（缺 `-n`、`-i` 误用、管道逻辑不完整） |

> 📌 **HW-03 从 Day 6 的"个别不精准"升级为 Day 7 的"系统性符号混淆"**，建议创建一个「Linux 易错大小写对照卡」贴在显示器旁边，每次用 grep/sed/awk 前扫一眼。

> 🎯 **整体评价**：第七天是 Linux 学习以来**挑战最大的一天**——三剑客的语法规则比文件操作命令复杂得多，20 题中出现了多个同类型的符号/大小写错误。但这**不是能力问题，是熟练度问题**。
>
> 你的强项非常明显：**grep 基础搜索**（Q1~4 全对）、**grep 选项理解**（自检中 8 个选项全部正确解释）、**awk 列提取概念**（知道 `$1`/`$4`/`$NF` 的含义）。这说明三剑客的**思维模型**你已经建立起来了——`grep` 找、`sed` 改、`awk` 算。
>
> 需要的是**语法肌肉记忆**：在终端里把每个出错的命令亲手敲一遍，看着报错信息，再把正确版本敲一遍。20 分钟终端练习的效果，远远大于 2 小时看文档。
>
> 修正以上错误后，建议在进入第八天之前，在终端里完成以下"康复训练"：
> ```bash
> echo -e "ERROR: timeout\nINFO: ok\nERROR: crash" | grep -B 1 "ERROR"
> echo -e "ERROR: timeout\nINFO: ok" | awk '$1 == "ERROR" {print $NF}'
> echo "hello world" | sed -n '1p'
> seq 1 10 | awk '{sum+=$1} END {print sum}'
> ```
> 4 个命令覆盖了今天所有错误类型，每条敲一遍只需要 2 分钟。第八天见！🚀

---
