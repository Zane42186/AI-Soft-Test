# 012对话总结

## 1. 总结区间说明
- **是否增量总结**：是
- **截取范围**：上次总结(011总结.md)后新增内容，第 17~21 轮

## 2. 对话核心内容概括

### 用户主要诉求
- 指出 011 总结应为增量而非全量
- 要求将本次对话的经验教训整理为文档，供后续新对话参考

### AI 核心操作/回答
- 承认错误：010 总结后 `lastConversationId` 写错了（填了上一个对话的 ID），导致后续 011 被误判为新对话
- 创建了 `协作协议/经验教训.md`，收录 6 条关键教训：
  1. PowerShell 文件编码（-Raw 不加 -Encoding UTF8 必乱码）
  2. Git 命令的误导性错误输出（NativeCommandError 不等于失败）
  3. Conclusion-Skill 状态文件更新（lastConversationId 必须等于 currentConversationId）
  4. .agents/ 目录的沙盒写入限制（通过 .claude/ 同步）
  5. Git 换行符警告无害（LF→CRLF）
  6. 项目架构速记（大脑双手在同一对话）

### 产生的文件 / 代码
| 文件 | 用途 |
|------|------|
| 协作协议/经验教训.md | 记录本项目已知的陷阱和规避方法 |

## 3. 关键信息提取
- **用户核心需求**：修正总结模式判断，记录经验教训
- **核心操作/方案**：
  - 修正 `lastConversationId` 使其等于 `currentConversationId`
  - 经验教训文档作为协作协议的子文档，新对话时一并读取
- **遗留问题 / 待跟进**：
  - 后续 Codex 新对话可通过 STATUS.md + 经验教训.md 快速上手套避坑
