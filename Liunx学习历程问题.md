# Linux 学习历程问题追踪

> 📦 本文档记录 Linux 模块学习过程中的练习评审、错误分析与改进建议。
> 对应教材：[Liunx.md](Liunx.md)

---

## 📋 从前序模块（功能测试 Days 1~4）继承的持续关注项

> 以下是在功能测试学习阶段暴露的弱项，在 Linux 模块学习中需持续关注和改进：

| 编号 | 弱项 | 表现 | 在 Linux 中如何体现 |
|------|------|------|---------------------|
| 🔴 **HW-01** | **边界方向混淆** | 下边界的内点/离点方向反复搞反（Day 2 金额 50.00、Day 3 金额 200） | Linux 命令中的数值参数（如 `head -n`、`tail -n`、`chmod` 权限位）需精确理解起始位置 |
| 🔴 **HW-02** | **严重程度判断** | 涉及资金/核心功能的 Bug 误判为 S4（轻微） | Linux 中涉及 `rm -rf`、`chmod 777` 等危险操作也需判断风险等级 |
| 🟡 **HW-03** | **术语精准度** | 八大要素名称不准（"测试标题"→"用例标题"、"代表数据"→"测试数据"） | Linux 命令英文全称记忆（如 `ls` = List、`chmod` = Change Mode） |
| 🟡 **HW-04** | **用例完整性** | 等价类只考虑范围、不考虑数据类型格式 | Linux 命令学习中注意覆盖各种参数组合和边界情况 |

> 📌 以上弱项的详细评审记录见 [学习历程问题.md](学习历程问题.md)

---

# 第六天：Linux 文件系统与基本操作

## 评审概要

| 练习 | 状态 | 主要问题 |
|------|------|----------|
| 练习一：命令匹配 | ⚠️ 有 3 处需修正 | `ll` 非标准命令、`less` 缺少搜索步骤、管道符多余反斜杠 |
| 练习二：权限解读 | ⚠️ 有 1 处需修正 | scripts 的其他人权限漏了"读"（r-x 不是 x） |
| 练习三：模拟排查日志 | ⚠️ 有 2 处可改进 | `ls -ltr` 缺少 `-h`、第 5 题文件名不一致 |
| 自检清单 | ⚠️ 仅填写了 2/13 项 | 大部分自检项留空未作答 |

---

## 逐题评审

### 练习一：命令匹配

| 题号 | 场景 | 你的答案 | 评审 |
|------|------|----------|------|
| 1 | 想知道当前在哪个目录 | `pwd` | ✅ **正确。** `pwd` = Print Working Directory，最直接的定位命令。 |
| 2 | 查看 `.log` 文件并显示大小 | `ll /var/log/*.log` | ⚠️ **有 2 个小问题。** ① `ll` 是 `ls -l` 的别名（alias），在大部分发行版中默认配置了但**不是标准命令**——面试和正式脚本中应写 `ls -l`；② 题目要求"显示文件大小"，配合 `-h`（human-readable）更佳：`ls -lh /var/log/*.log` 可以把 2048 字节显示为 `2.0K`。 |
| 3 | 进入 `/opt/app/config` | `cd /opt/app/config` | ✅ **正确。** 使用了绝对路径，标准写法。 |
| 4 | 创建 data 和 logs 目录 | `mkdir -p /tmp/test/{data,logs}` | ⭐ **超出预期！** 同时使用了 `-p`（递归）和 `{ }` 花括号展开（批量创建），一行搞定，非常优雅。 |
| 5 | 复制配置文件并备份 `.bak` | `cp file.txt file.txt.bak` | ✅ **语法正确。** 用 `cp` 生成 `.bak` 备份是标准操作。💡 小提示：实战中文件名应具体（如 `cp config.yml config.yml.bak`），不然别人不知道你备份了哪个文件。 |
| 6 | 实时监控日志看新错误 | `tail -f app.log \| grep "error"` | ⚠️ **有 2 个小问题。** ① 管道符前的反斜杠 `\|` 多余——在 bash 中直接用 `|` 即可，`\|` 会在某些 Shell 中被当作转义；② 搜索关键词 `"error"` 是小写，常见的 ERROR 日志是大写，建议用 `grep -i "error"`（忽略大小写）或直接用 `grep "ERROR"`。 |
| 7 | 翻看 500MB 日志文件，搜索 "NullPointerException" | `less app.log` | ⚠️ **不完整。** `less app.log` 只是"打开了文件"，**缺少搜索步骤**。题目要求搜索特定异常名，完整操作是：`less app.log` 然后按 `/NullPointerException` 回车开始搜索，按 `n` 跳下一个。建议答案写为：`less app.log` → `/NullPointerException`。 |
| 8 | 查看配置文件前 20 行 | `head -n 20 config.yml` | ✅ **正确。** `head -n` 精确指定行数，标准用法。 |

---

### 练习二：权限解读

#### 权限位速查

```
drwxr-xr-x  → scripts  |  -rwxr--r--  → deploy.sh
-rw-------  → secret.key |  drwxr-x---  → logs
```

| 文件 | 类型 | 所有者权限 | 组权限 | 其他人权限 | 含义 | 评审 |
|------|------|-----------|--------|-----------|------|------|
| scripts | 目录 | 读-写-执行 | 读-执行 | 执行 | 目录，所有者可以进入和查看并创建和删除目录下文件，组成员可以查看目录下文件和进入目录，其他人只可进入 | ⚠️ **其他人权限漏了"读"。** `r-x` 包含 **r（读）** 和 x（执行），不只是执行。正确：其他人可以**查看目录内容**（ls）并可进入目录（cd）。 |
| deploy.sh | 文件 | 读写执行 | 读 | 读 | 文件，所有者可查看修改执行文件，其他人和组成员只可读 | ✅ **正确。** `rwx` = 读+写+执行，`r--` = 只读。 |
| secret.key | 文件 | 读写 | 无 | 无 | 文件，所有者可查看和修改文件，其他人无权限 | ✅ **正确。** `rw-------` = 只有所有者可读写，组和其他人完全无权限。这是私钥/密钥文件的最佳实践权限。 |
| logs | 目录 | 读写执行 | 读执行 | 无 | 目录，所有者可查看和进入目录，并可以在目录下创建删除目录和文件。组成员可查看和进入目录，其他人无权限 | ✅ **完美。** `rwx` + `r-x` + `---` 解读精准，含义描述覆盖了所有三类用户的权限。 |

#### 修正对照：scripts 的其他人权限

```
   r  -  x
   │     │
   │     └── 执行（x）：可以 cd 进入该目录
   └── 读（r）：可以 ls 查看目录内容

你的答案："只可进入" → 只有 x
正确答案："可查看目录内容并可进入" → r + x
```

---

### 练习三：模拟排查日志

| 题号 | 你的答案 | 评审 |
|------|----------|------|
| 1. 进入日志目录 | `cd /var/log/myapp/` | ✅ **正确。** 绝对路径进入，标准操作。 |
| 2. 查看文件列表（按时间排序 + 大小） | `ls -ltr` | ⚠️ **功能正确但缺细节。** `-ltr` 确实按时间排序（`-t`）且反向显示（`-r`，最新的在最下面），也显示了文件大小（`-l` 的第 5 列）。但缺少 `-h`（human-readable）：`ls -ltrh` 会把 `2097152` 显示为 `2.0M`，可读性显著提升。 |
| 3. 今天的日志文件 + 查看最后 50 行 | `app-2026-07-08.log tail -n 50 app-2026-07-08.log` | ✅ **正确。** 文件名 `app-2026-07-08.log` 格式完全正确（今天 = 2026-07-08），`tail -n 50` 用法标准。💡 格式提示：文件名和命令写在了同一行但没有分隔符，建议分行写或加一个空格分号。 |
| 4. 实时监控 ERROR | `tail -f app.log \| grep "ERROR"` | ✅ **正确。** 管道 + `grep` 过滤是测试工程师最常用的日志监控组合。⚠️ 小问题同练习一：`\|` 的反斜杠多余，直接 `|` 即可。 |
| 5. 搜索并统计 Timeout | `grep -c "Timeout" app.log` | ⭐ **超出预期！** 你直接用了下一天才会讲的 `grep -c`（count 模式），而且语法完全正确。这说明你提前查了资料，主动学习能力很棒。💡 小提示：文件名写的是 `app.log`，但第 3 题中今天的日志文件名是 `app-2026-07-08.log`——实战中要保持文件名一致，避免搜错文件。 |

---

## 📋 自检清单评审

| 自检项 | 你的回答 | 评审 |
|--------|----------|------|
| 测试工程师学 Linux 的 3 个场景 | ① 大部分软件在 Linux 服务器下 ② 查看日志定位 bug ③ 运行自动化脚本 ④ 部署环境 | ⭐ **列出 4 个，超出要求！** 覆盖了教材中的核心场景。 |
| 终端提示符各部分含义 | ① user=用户名 ② host=主机名 ③ ~=当前目录 ④ $=普通用户 | ✅ **4/4 完全正确。** 解读精准，`~` 和 `$` 的含义都到位。 |
| 如何查看命令帮助 | （未填写） | ⚠️ **建议补充。** 答案应为：`命令 --help`（简要帮助）或 `man 命令`（详细手册）。这是自学 Linux 最重要的元技能。 |
| 基本目录导航命令 | （未填写） | ⚠️ **建议补充。** 答案应为：`pwd`（查看位置）、`ls`（列出内容）、`cd`（切换目录）。 |
| 绝对路径 vs 相对路径 | （未填写） | ⚠️ **建议补充。** 绝对路径从 `/` 开始（如 `/var/log`），相对路径从当前目录开始（如 `./logs` 或 `../logs`），类比 GPS 坐标 vs "往前走右转"。 |
| mkdir -p 和 { } 用法 | （未填写） | ⚠️ **建议补充。** 练习一中你已正确使用了 `mkdir -p /tmp/test/{data,logs}`，说明掌握了！只需在自检这里确认一下。 |
| rm -rf 风险认知 | （未填写） | ⚠️ **建议补充。** 答案应为：`rm -rf /` 会删除整个系统，`rm -rf *` 删除当前目录全部内容。安全习惯：先 `ls` 确认再 `rm`，或用 `rm -i` 开启确认。 |
| cp 备份、mv 重命名 | （未填写） | ⚠️ **建议补充。** 练习一第 5 题已正确使用 `cp` 备份，说明掌握了。 |
| cat/less/head/tail 场景区分 | （未填写） | ⚠️ **建议补充。** `cat`=小文件全文，`less`=大文件分页浏览，`head`=看开头 N 行，`tail`=看末尾 N 行 + 实时跟踪。 |
| tail -f 实时跟踪 | （未填写） | ⚠️ **建议补充。** 练习一第 6 题和练习三第 4 题已正确使用，说明掌握了。 |
| less 搜索和翻页 | （未填写） | ⚠️ **建议补充。** 翻页：Space/PgDn 向下、b/PgUp 向上；搜索：`/关键词` 向下搜、`?关键词` 向上搜、`n` 下一个、`N` 上一个。 |
| 权限 -rw-r--r-- 解读 | （未填写） | ⚠️ **建议补充。** 练习二已练习过，应能说出：`-`=普通文件，`rw-`=所有者可读写，`r--`=组只读，`r--`=其他人只读。 |

> 📌 自检清单 13 项中你只填写了 2 项。虽然**练习题的完成度很高**（说明核心技能你确实掌握了），但自检清单的目的是**让你用自己的话复述一遍**，这能帮你发现"我以为会了但其实说不清楚"的知识点。建议花 10 分钟把空着的 11 项补上，不需要长篇大论，一两句话就行。

---

## 总结

| 评价维度 | 结果 |
|----------|------|
| **练习一** | 8 题中 4 题完全正确、3 题有小瑕疵（`ll` 非标准、`less` 缺搜索、管道转义）、1 题语法正确但可优化 |
| **练习二** | 4 个文件中 3 个权限解读完全正确、1 个漏了其他人"读"权限 |
| **练习三** | 5 题中 3 题完全正确（含 1 题超出预期）、2 题有细节可改进 |
| **自检清单** | 2/13 项已填写，已填部分质量高，11 项建议补充 |

### 需要修正的重点

1. 🔴 **（必须理解）权限 r-x ≠ x**：练习二中 scripts 的其他人权限是 `r-x`（可读可进入），不是"只可进入"（x）。这个错误和之前功能测试模块的"看漏条件"（HW-04）一脉相承——读到后半截 `x` 就忘了前面的 `r`。🔧 **记忆法**：权限三位一体，读权限（r）永远在第一位，只要看到 `r-x` 就要想到"有 r"。

2. 🟡 **（建议）放弃 `ll`，习惯 `ls -l`**：`ll` 是别名不是标准命令，面试写 `ll` 可能被追问，脚本中 `ll` 可能不存在。从现在起统一用 `ls -l` / `ls -lh`。

3. 🟡 **（建议）补全自检清单**：11 项空白中有多项已在练习中证明你掌握了（如 `mkdir -p`、`tail -f`、权限解读），只需花 10 分钟用自己的话写一两句即可——这会让你对 Linux 第一天的知识体系有一个完整回顾。

4. 🟡 **（建议）less 搜索是"两步操作"**：`less` 打开文件只是第一步，搜索 `/关键词` 才是第二步。以后题目问"搜索某个内容的命令"，一定要把搜索步骤写全。

### 跨模块追踪

| 历史弱项 | 本次是否复现 | 说明 |
|----------|-------------|------|
| 🔴 HW-01 边界方向混淆 | 未复现 ✅ | 本次不涉及边界值判断 |
| 🔴 HW-02 严重程度判断 | 未复现 ✅ | 本次不涉及缺陷分级 |
| 🟡 HW-03 术语精准度 | ⚠️ **部分复现** | `ll` 代替 `ls -l`、scripts 权限漏"读"——都是不精准的表现 |
| 🟡 HW-04 用例完整性 | 未复现 ✅ | 本次练习完成度整体良好 |

> 🎯 **整体评价**：第六天 Linux 开局很不错！练习一和练习三的核心命令（`pwd`、`cd`、`mkdir -p`、`tail -f`、`head -n`、`grep -c`）用得都很熟，尤其是 `mkdir -p /tmp/test/{data,logs}` 和 `grep -c` 已经是**加分项级别**的表现。权限解读也基本到位（4 中 3 正确），说明你对 `rwx` 体系的理解框架是对的。
>
> 今天最大的收获是：你已经能**把命令和测试场景对应起来**——知道查日志用 `tail -f`、找错误用 `grep`、翻大文件用 `less`——这才是测试工程师学 Linux 的核心价值，不是背命令而是**知道什么场景用什么工具**。
>
> 把自检清单补上、权限漏读那个点纠正一下，就可以进入第七天了！第七天的文本三剑客（`grep`/`sed`/`awk`）是你今天已经提前接触过的 `grep` 的深度展开，会很过瘾。🚀

---
